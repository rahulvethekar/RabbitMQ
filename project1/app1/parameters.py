
#Payload send to middleware api to consume data of app2
payload = {
            'url':'employee/list/',
            'method':'GET',
        }

#Base url of api
BASE_URL = "http://127.0.0.1:8001"

# Api end point
END_POINT = 'api/middleware/'

